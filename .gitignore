node_modules/
dist/
.next/
.turbo/
*.log
.env
.env.*
!.env.example
docker-compose.local.yml
local-dev.ps1
*.tsbuildinfo
.DS_Store
Thumbs.db
/apps/admin/.next/
/apps/api/dist/
/packages/db/migrations/meta/
drizzle/

# User-uploaded chat attachments — never belongs in the repo
apps/api/uploads/

# Local agent/editor scaffolding
.claude/

# Expo / React Native
.expo/
apps/mobile-patient/.expo/
apps/mobile-doctor/.expo/
*.apk
# APK лендинга (apps/landing/downloads/) живут только на VPS и выкладываются
# через scp, минуя git — см. docs/apk-release.md. Не добавлять через -f.
# version.json рядом с ними генерируется на VPS из самих APK.
apps/landing/downloads/version.json
*.ipa
*.aab

# Playwright e2e: артефакты прогона. Сам набор тестов в репозитории, а
# скриншоты и отчёты пересоздаются каждым запуском — Farhod смотрит их
# локально в e2e/screenshots/.
e2e/screenshots/
e2e/report/
